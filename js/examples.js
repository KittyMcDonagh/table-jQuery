$("h2").addClass("underline");   // underline all h2 elements
    $("ul").addClass("border");       // underline all list items
    
    $("h2").removeClass("underline");   // remove the class all h2 elements
    $("ul").removeClass("border");       // remove the class all list items